spring:
  redis:
    database: 0 # 数据库索引
    host: 47.92.86.79 # 连接地址
    port: 6379 # 默认连接端口
    password: sunfounder
    lecttuce:
      shutdown-timeout: 200ms
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0
    jedis:
      pool:
        max-active: 20 # 最大连接
        max-idle: 8    # 最大空闲连接
        min-idle: 1    # 最小空闲连接

